<h1>
	Dashboard
</h1>

<h3>
	Welcome Back, {{user.name}}
</h3>

<ul>
	{{log tickets}}
	{{log user}}
	{{#each tickets as |ticket|}}
		<li>
			<h2>
				{{ticket.title}}
			</h2>
			<p>
				{{ticket.description}}
			</p>
			<p>
				Created by: {{ticket.creator.name}}
			</p>
		</li>
	{{/each}}
</ul>